import { ServiceType } from './constant';
/**
 *  서비스 별 토큰 관리 클래스
 */
export declare class AccessToken {
    private sandbox;
    private demo;
    private api;
    constructor();
    setToken(serviceType: ServiceType, accessToken: string): void;
    getToken(serviceType: ServiceType): string;
}
