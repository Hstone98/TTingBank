import { ServiceType } from './constant';
export declare type ClientInfo = {
    clientID: string;
    clientSecret: string;
};
export declare class Properties {
    private publicKey;
    private clientID;
    private clientSecret;
    private demoClientID;
    private demoClientSecret;
    private readonly sandboxClientID;
    private readonly sandboxClientSecret;
    constructor();
    /**
     * 정식서버 사용을 위한 클라이언트 정보 설정
     * @param clientID
     * @param clientSecret
     */
    setClientInfo(clientID: string, clientSecret: string): void;
    /**
     * 데모서버 사용을 위한 클라이언트 정보 설정
     * @param clientID
     * @param clientSecret
     */
    setDemoClientInfo(clientID: string, clientSecret: string): void;
    /**
     * 상품 요청 URL
     * @param productURLPath
     * @returns {string}
     */
    getProductRequestURL(serviceType: ServiceType, productURLPath: string): string;
    /**
     * RSA암호화를 위한 퍼블릭키 설정
     * @param publicKey
     */
    setPublicKey(publicKey: string): void;
    getPublicKey(): string;
    /**
     * 클라이언트 정보 조회
     * @param {ServiceType} serviceType
     */
    getClientInfo(serviceType: ServiceType): ClientInfo;
}
